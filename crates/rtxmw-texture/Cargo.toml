[package]
name = "rtxmw-texture"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[features]
# Hand-built textures for tests in crates above this one.
internals = []

[lints]
workspace = true

[dev-dependencies]
rtxmw-vfs = { workspace = true, features = ["internals"] }
